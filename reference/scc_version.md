# Return the installed scc version string

Return the installed scc version string

## Usage

``` r
scc_version()
```

## Value

Character scalar, e.g. `"scc version 3.7.0"`.

## Examples

``` r
if (FALSE) { # \dontrun{
scc_version()
} # }
```
