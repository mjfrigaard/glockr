% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scc_cocomo.R
\name{scc_cocomo}
\alias{scc_cocomo}
\title{Build a COCOMO summary tibble from scc's tabular output}
\usage{
scc_cocomo(
  scc_bin,
  path,
  avg_wage = NULL,
  cocomo_project_type = NULL,
  eaf = NULL,
  overhead = NULL,
  currency_symbol = NULL,
  sloccount_format = FALSE,
  auto_print = TRUE
)
}
\arguments{
\item{scc_bin}{Path to the \code{scc} executable.}

\item{path}{Character vector of paths passed to \code{scc}.}

\item{avg_wage, cocomo_project_type, eaf, overhead, currency_symbol, sloccount_format}{COCOMO-related arguments; see \code{\link[=scc]{scc()}} for descriptions.}

\item{auto_print}{Logical. When \code{sloccount_format = TRUE} the upstream
block has a different shape than the default 3-line summary; if
\code{auto_print} is \code{TRUE} it is printed verbatim and the function returns
\code{NULL} (since it cannot be coerced to the standard tibble).}
}
\value{
A 3-row \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{metric}, \code{project_type},
\code{value}, or \code{NULL} for the sloccount-format / unparseable cases.
}
\description{
Runs \code{scc} a second time in tabular mode and extracts the COCOMO cost /
effort block, which \code{scc} only emits for its tabular formatter (not for
\verb{--format json}). The three default-format lines are parsed into a
\code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{metric}, \code{project_type}, \code{value}.
}
\details{
Called by \code{\link[=scc]{scc()}} and \code{\link[=scc_by_file]{scc_by_file()}} only when \code{cocomo = TRUE}.
}
\keyword{internal}
