% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_args.R
\name{build_args}
\alias{build_args}
\title{Build the scc argument vector}
\usage{
build_args(
  sort,
  no_complexity,
  no_duplicates,
  no_gen,
  include_ext,
  exclude_ext,
  not_match,
  by_file
)
}
\arguments{
\item{sort}{Character. Column to sort by. One of \code{"name"}, \code{"files"},
\code{"lines"}, \code{"code"}, \code{"comments"}, \code{"blanks"}, \code{"complexity"}.}

\item{no_complexity}{Logical. Add \code{--no-complexity} flag when \code{TRUE}.}

\item{no_duplicates}{Logical. Add \code{--no-duplicates} flag when \code{TRUE}.}

\item{no_gen}{Logical. Add \code{--no-gen} flag when \code{TRUE}.}

\item{include_ext}{Character vector of extensions to include, or \code{NULL}.
Multiple extensions are collapsed to a comma-separated string.}

\item{exclude_ext}{Character vector of extensions to exclude, or \code{NULL}.
Multiple extensions are collapsed to a comma-separated string.}

\item{not_match}{Character vector of regex patterns. Each becomes its own
\verb{--not-match <pattern>} pair in the output, or \code{NULL} for none.}

\item{by_file}{Logical. Add \code{--by-file} flag when \code{TRUE}.}
}
\value{
Character vector of CLI arguments, always starting with
\verb{c("--format", "json", "--sort", <sort>)}.
}
\description{
Translates R-level options into the character vector of CLI flags passed
to \code{scc} via \code{\link[processx:run]{processx::run()}}.
}
\examples{
\dontrun{
# Default language-level summary
build_args("code", FALSE, FALSE, FALSE, NULL, NULL, NULL, FALSE)

# Per-file, R only, skip complexity
build_args("lines",
  no_complexity = TRUE,
  no_duplicates = FALSE,
  no_gen        = FALSE,
  include_ext   = "r",
  exclude_ext   = NULL,
  not_match     = NULL,
  by_file       = TRUE
)
}
}
