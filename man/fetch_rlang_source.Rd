% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_rlang_source.R
\name{fetch_rlang_source}
\alias{fetch_rlang_source}
\title{Fetch the rlang package source tree for vignette demos}
\usage{
fetch_rlang_source(
  version = "1.1.6",
  host = "https://api.github.com",
  timeout = 5L
)
}
\arguments{
\item{version}{Character. rlang release tag without the leading \code{"v"}.}

\item{host}{Character. URL probed by the internal connectivity check.}

\item{timeout}{Integer. Seconds to wait for the connectivity check
before giving up.}
}
\value{
Character scalar — the absolute path of the extracted rlang
source directory on success, or \code{NA_character_} when no network is
available or the download / untar step fails.
}
\description{
Downloads a tagged release tarball of the rlang package from GitHub and
extracts it into a session-local tempdir, returning the path to the
extracted directory. Subsequent calls within the same R session reuse
the cached extraction. This is used by the package vignettes so they can
point \code{scc()} at a real R package without bundling rlang's source with
the installed glockr package.
}
\details{
The vignette setup chunks typically use it like:

\if{html}{\out{<div class="sourceCode">}}\preformatted{rlang_path <- glockr:::fetch_rlang_source()
if (is.na(rlang_path)) \{
  knitr::opts_chunk$set(eval = FALSE)
  rlang_path <- ""
\}
}\if{html}{\out{</div>}}
}
\keyword{internal}
