% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_scc_json.R
\name{parse_scc_json}
\alias{parse_scc_json}
\title{Parse JSON output from scc into a tibble}
\usage{
parse_scc_json(json_text, by_file = FALSE)
}
\arguments{
\item{json_text}{Character scalar: raw stdout captured from \code{scc}.}

\item{by_file}{Logical. When \code{TRUE} unpacks the per-file \code{Files} array
inside each language block; when \code{FALSE} (default) returns one row per
language.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}}. Column layout matches \code{\link[=scc]{scc()}} when
\code{by_file = FALSE} and \code{\link[=scc_by_file]{scc_by_file()}} when \code{by_file = TRUE}.
}
\description{
Converts the raw JSON written to stdout by \verb{scc --format json} into a
\code{\link[tibble:tibble]{tibble::tibble()}}. Handles empty or blank output by returning a zero-row
tibble with the correct column schema via \code{\link[=empty_scc_tibble]{empty_scc_tibble()}}.
}
\examples{
\dontrun{
json <- processx::run("scc", c("--format", "json", "."))$stdout
parse_scc_json(json)

json_by_file <- processx::run("scc", c("--format", "json", "--by-file", "."))$stdout
parse_scc_json(json_by_file, by_file = TRUE)
}
}
