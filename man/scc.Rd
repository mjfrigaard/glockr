% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scc.R
\name{scc}
\alias{scc}
\title{Count lines of code by language}
\usage{
scc(
  path = ".",
  sort = "code",
  no_complexity = FALSE,
  uloc = FALSE,
  dryness = FALSE,
  character = FALSE,
  percent = FALSE,
  size_unit = NULL,
  verbose = FALSE,
  debug = FALSE,
  no_duplicates = FALSE,
  binary = FALSE,
  gen = FALSE,
  no_gen = FALSE,
  min = FALSE,
  no_min = FALSE,
  min_gen = FALSE,
  no_min_gen = FALSE,
  min_gen_line_length = NULL,
  generated_markers = NULL,
  include_ext = NULL,
  exclude_ext = NULL,
  exclude_dir = NULL,
  exclude_file = NULL,
  not_match = NULL,
  count_as = NULL,
  remap_all = NULL,
  remap_unknown = NULL,
  include_symlinks = FALSE,
  no_large = FALSE,
  large_byte_count = NULL,
  large_line_count = NULL,
  count_ignore = FALSE,
  no_gitignore = FALSE,
  no_gitmodule = FALSE,
  no_ignore = FALSE,
  no_scc_ignore = FALSE,
  no_hborder = FALSE,
  no_size = FALSE,
  no_cocomo = FALSE,
  avg_wage = NULL,
  cocomo_project_type = NULL,
  eaf = NULL,
  overhead = NULL,
  currency_symbol = NULL,
  sloccount_format = FALSE,
  directory_walker_job_workers = NULL,
  file_gc_count = NULL,
  file_list_queue_size = NULL,
  file_process_job_workers = NULL,
  file_summary_job_queue_size = NULL
)
}
\arguments{
\item{path}{Character vector of file/directory paths to analyse.
Defaults to the current working directory (\code{"."}).}

\item{sort}{Column to sort by. One of \code{"name"}, \code{"files"}, \code{"lines"},
\code{"code"}, \code{"comments"}, \code{"blanks"}, \code{"complexity"}. Default \code{"code"}.}

\item{no_complexity}{Logical. Skip complexity calculation (\code{-c}).}

\item{uloc}{Logical. Calculate unique lines of code (\code{-u}).}

\item{dryness}{Logical. Calculate project DRYness; implies \code{uloc} (\code{-a}).}

\item{character}{Logical. Calculate max/mean characters per line (\code{-m}).}

\item{percent}{Logical. Include percentage values in tabular output (\code{-p}).
No effect on the returned tibble.}

\item{size_unit}{Character. Set size unit for display. One of \code{"si"},
\code{"binary"}, \code{"mixed"}, or an \code{"xkcd-*"} variant.  Default \code{NULL} (scc
default \code{"si"}).}

\item{verbose}{Logical. Verbose output (\code{-v}).}

\item{debug}{Logical. Enable debug output.}

\item{no_duplicates}{Logical. Remove duplicate files from stats (\code{-d}).}

\item{binary}{Logical. Disable binary-file detection.}

\item{gen}{Logical. Identify generated files.}

\item{no_gen}{Logical. Ignore generated files; implies \code{gen}.}

\item{min}{Logical. Identify minified files.}

\item{no_min}{Logical. Ignore minified files; implies \code{min}.}

\item{min_gen}{Logical. Identify minified or generated files (\code{-z}).}

\item{no_min_gen}{Logical. Ignore minified or generated files; implies
\code{min_gen}.}

\item{min_gen_line_length}{Integer. Bytes per average line threshold for
minified/generated detection. Default \code{NULL} (scc default \code{255}).}

\item{generated_markers}{Character vector. Strings in the head of a file
that mark it as generated. Passed comma-separated. Default \code{NULL}
(scc defaults: \code{c("do not edit", "<auto-generated />")}).}

\item{include_ext}{Character vector. Limit to these file extensions
(comma-separated in one flag, e.g. \code{c("go", "java")}) (\code{-i}).}

\item{exclude_ext}{Character vector. Ignore these file extensions; overrides
\code{include_ext} (\code{-x}).}

\item{exclude_dir}{Character vector. Directories to exclude. Default \code{NULL}
(scc defaults: \code{.git}, \code{.hg}, \code{.svn}).}

\item{exclude_file}{Character vector. Ignore files with matching names
(\code{-n}). Default \code{NULL} (scc defaults: lock files).}

\item{not_match}{Character vector. Ignore files/directories whose path
matches any regex pattern. Each element becomes its own \code{--not-match}
flag (\code{-M}).}

\item{count_as}{Character. Map extensions to languages, e.g.
\code{"jsp:htm,chead:C Header"}.}

\item{remap_all}{Character. Inspect every file and remap by string match,
e.g. \code{"-*- C++ -*-:C Header"}.}

\item{remap_unknown}{Character. Like \code{remap_all} but only for unknown-type
files.}

\item{include_symlinks}{Logical. Count symlinked files.}

\item{no_large}{Logical. Ignore files exceeding \code{large_byte_count} /
\code{large_line_count}.}

\item{large_byte_count}{Integer. Byte threshold for large-file removal.
Default \code{NULL} (scc default \code{1000000}).}

\item{large_line_count}{Integer. Line threshold for large-file removal.
Default \code{NULL} (scc default \code{40000}).}

\item{count_ignore}{Logical. Count \code{.gitignore} / \code{.ignore} files.}

\item{no_gitignore}{Logical. Disable \code{.gitignore} logic.}

\item{no_gitmodule}{Logical. Disable \code{.gitmodules} logic.}

\item{no_ignore}{Logical. Disable \code{.ignore} logic.}

\item{no_scc_ignore}{Logical. Disable \code{.sccignore} logic.}

\item{no_hborder}{Logical. Remove horizontal borders from tabular output.
No effect on the returned tibble.}

\item{no_size}{Logical. Remove size-calculation line from tabular output.
No effect on the returned tibble.}

\item{no_cocomo}{Logical. Remove COCOMO output from tabular display.}

\item{avg_wage}{Integer. Average annual wage for COCOMO calculation.
Default \code{NULL} (scc default \code{56286}).}

\item{cocomo_project_type}{Character. COCOMO model type: \code{"organic"},
\code{"semi-detached"}, \code{"embedded"}, or \code{"custom,a,b,c,d"}. Default \code{NULL}
(scc default \code{"organic"}).}

\item{eaf}{Numeric. Effort adjustment factor for COCOMO. Default \code{NULL}
(scc default \code{1}).}

\item{overhead}{Numeric. Corporate overhead multiplier for COCOMO. Default
\code{NULL} (scc default \code{2.4}).}

\item{currency_symbol}{Character. Symbol used in COCOMO cost display.
Default \code{NULL} (scc default \code{"$"}).}

\item{sloccount_format}{Logical. Print SLOCCount-style COCOMO output.}

\item{directory_walker_job_workers}{Integer. Workers for directory walking.
Default \code{NULL} (scc default \code{8}).}

\item{file_gc_count}{Integer. Files parsed before GC is enabled. Default
\code{NULL} (scc default \code{10000}).}

\item{file_list_queue_size}{Integer. Queue size for discovered files.
Default \code{NULL} (scc default \code{12}).}

\item{file_process_job_workers}{Integer. Workers for file-stat collection.
Default \code{NULL} (scc default \code{12}).}

\item{file_summary_job_queue_size}{Integer. Queue size for processed-file
stats. Default \code{NULL} (scc default \code{12}).}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per language and columns:
\describe{
\item{language}{Programming language name.}
\item{files}{Number of files (integer).}
\item{lines}{Total line count (integer).}
\item{code}{Lines of code (integer).}
\item{comments}{Lines of comments (integer).}
\item{blanks}{Blank lines (integer).}
\item{complexity}{Total cyclomatic complexity score (integer).}
\item{weighted_complexity}{Complexity weighted by lines of code (double,
rounded to 2 decimal places).}
\item{bytes}{File size in bytes (integer).}
\item{uloc}{Unique lines of code; \code{0} unless \code{uloc} or \code{dryness} is
\code{TRUE} (integer).}
}
}
\description{
Runs \code{scc} on one or more paths and returns a per-language summary.
}
\examples{
\dontrun{
scc()
scc("~/myproject", sort = "lines", no_complexity = TRUE)
scc(".", uloc = TRUE, exclude_dir = c("tests", "vignettes"))
scc(".", no_gen = TRUE, no_min = TRUE, no_duplicates = TRUE)
}
}
